import 'package:flutter/material.dart';

class JournalSubmissionsPage extends StatelessWidget {
  const JournalSubmissionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Journal Submissions Page Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}