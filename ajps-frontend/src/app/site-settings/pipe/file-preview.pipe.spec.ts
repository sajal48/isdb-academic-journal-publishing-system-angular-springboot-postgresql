import { FilePreviewPipe } from './file-preview.pipe';

describe('FilePreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new FilePreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
